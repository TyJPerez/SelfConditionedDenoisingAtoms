

load_model: null
log_dir: experiments/


## DATASET & LOADER INPUTS ###
dataset: PCQM4MV2
dataset_root: tmp/pcq
dataset_arg : null
train_size: 1000 #null #DEBUG
test_size: 100
val_size: 100
batch_size: 64
inference_batch_size: 70
seed: 1
num_workers: 6

model_config: configs/model_configs/et_cls.yaml

denoising_only: true 
energy_weight: 0.0
force_weight: 1.0
ema_alpha_dy: 1.0
ema_alpha_y: 1.0

distributed_backend: ddp
early_stopping_patience: 150
ngpus: -1
num_nodes: 1
redirect: false

lr: 0.0004
lr_schedule: cosine
lr_factor: 0.8
lr_min: 1.0e-07
lr_patience: 15
lr_warmup_steps: 10000 
lr_cosine_length: 400000
num_epochs: 30 # DEBUG
num_steps: 400000 #DEBUG
precision: 32

save_interval: 1
test_interval: 1

splits: null
standardize: false ###????? multiply prediction by dataset std and add mean

wandb_notes: ""
job_id: auto

pretraining: true
self_cond: false
weight_decay: 0.0

# data agumentations
random_rotate: false
torsion_angle_std: 0
max_bonds_rotated : 0
p_invert : 0.0
noise_scale: 0.04
denoising_weight: 1.0 